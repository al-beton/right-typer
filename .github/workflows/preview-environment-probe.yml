name: Preview environment isolation probe
on: pull_request
permissions: {}
jobs:
  must-be-blocked:
    runs-on: ubuntu-latest
    environment: pr-preview-publishing
    steps:
      - name: This must never execute from a PR
        run: exit 1
